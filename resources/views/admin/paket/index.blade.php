@extends('layouts.admin')
@section('title', 'Kelola Paket Wisata')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><h5 class="mb-0">Daftar Paket</h5><a href="{{ route('admin.paket.create') }}" class="btn btn-primary"><i class="fas fa-plus me-2"></i>Tambah</a></div>
<div class="card shadow-sm"><div class="card-body"><div class="table-responsive"><table class="table table-hover"><thead><tr><th>No</th><th>Nama</th><th>Destinasi</th><th>Harga</th><th>Stok</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($paketWisata as $p)<tr><td>{{ $paketWisata->firstItem()+$loop->index }}</td><td>{{ $p->nama_paket }}</td><td>{{ $p->destinasi }}</td><td>Rp {{ number_format($p->harga,0,',','.') }}</td><td><span class="badge {{ $p->stok>0?'bg-success':'bg-danger' }}">{{ $p->stok }}</span></td><td><span class="badge {{ $p->status==='aktif'?'bg-success':'bg-secondary' }}">{{ ucfirst($p->status) }}</span></td><td><a href="{{ route('admin.paket.edit',$p) }}" class="btn btn-sm btn-warning"><i class="fas fa-edit"></i></a> <form method="POST" action="{{ route('admin.paket.destroy',$p) }}" class="d-inline" onsubmit="return confirm('Hapus?')">@csrf @method('DELETE')<button class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button></form></td></tr>@empty<tr><td colspan="7" class="text-center">Belum ada paket</td></tr>@endforelse</tbody></table></div>{{ $paketWisata->links() }}</div></div>
@endsection
